{{- include "grafana.validateConfig" . -}}
apiVersion: dip.io/v1alpha1
kind: HelmApplication
metadata:
  name: grafana-helm
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: {{ .Values.argoProject | quote }}
  source:
    chart: grafana
    repoURL: https://grafana-community.github.io/helm-charts
    targetRevision: {{ .Values.grafanaChart.version }}
    helm:
      releaseName: {{ .Values.grafanaChart.releaseName }}
      valuesObject:
        {{- (tpl (.Files.Get "config/grafana-values.yaml") .) | trim | nindent 8 }}
  destination:
    namespace: {{ .Release.Namespace | quote }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
