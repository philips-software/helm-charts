{{ if .Values.multitenancyEnabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: compactor-orgmapper-cm
  namespace: tempo-system
data:
  config.yaml: |
{{ (tpl (.Files.Get "config/compactor-orgmapper.yaml") .) | trim | indent 4 }}
{{ end }}
