environmentConfig:
  resourcePrefix: ""

postgres:
  # Set to true to use an existing database (connection details secret must be pre-populated)
  useExisting: false
  sourceIdentifier: "masterdatadb-202601081341"

grafanaDatasource:
  enabled: true
  name: "Masterdata PostgreSQL"
  # URL, username, and password are patched by the Job from the connection secrets
  databaseName: "masterdatadb"
  isDefault: false
  sslMode: "require"
  providerConfigRef: "default"
