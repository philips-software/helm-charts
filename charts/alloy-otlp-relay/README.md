# alloy-otlp-relay

![Version: 0.2.0](https://img.shields.io/badge/Version-0.2.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 1.19.2](https://img.shields.io/badge/AppVersion-1.19.2-informational?style=flat-square)

Minimal Alloy instance that receives OTLP metrics, filters by an exact-name allow-list, and forwards to a Mimir gateway with a tenant header

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| livenessProbe.failureThreshold | int | `3` |  |
| livenessProbe.initialDelaySeconds | int | `10` |  |
| livenessProbe.periodSeconds | int | `10` |  |
| livenessProbe.timeoutSeconds | int | `5` |  |
| metricsFilter | object | `{"allowList":[]}` | Filters incoming metrics before they're forwarded. |
| metricsFilter.allowList | list | `[]` | Exact metric names to allow through. Empty list = pass all metrics through unfiltered. |
| mimir | object | `{"tenantId":"long-term","url":"http://mimir-gateway.mimir-system.svc.cluster.local/otlp"}` | Forwarding destination. |
| mimir.tenantId | string | `"long-term"` | Value injected as the X-Scope-OrgID header on every forwarded request. |
| mimir.url | string | `"http://mimir-gateway.mimir-system.svc.cluster.local/otlp"` | Base OTLP endpoint for the Mimir gateway. The otlphttp exporter appends /v1/metrics itself. |
| readinessProbe.failureThreshold | int | `3` |  |
| readinessProbe.initialDelaySeconds | int | `5` |  |
| readinessProbe.periodSeconds | int | `5` |  |
| readinessProbe.timeoutSeconds | int | `5` |  |
| receiver | object | `{"http":{"metricsPath":"/otlp/v1/metrics","port":4318}}` | OTLP/HTTP receiver settings. gRPC is intentionally not exposed. |
| receiver.http.metricsPath | string | `"/otlp/v1/metrics"` | Path the receiver accepts metrics on. Defaults to otlp-gateway's mirrored path rather than the OTLP-standard /v1/metrics: otlp-gateway's caddy-mirror plugin duplicates a request at whatever path it already rewrote for its own primary upstream (Mimir's native /otlp/v1/metrics), so that's the path this receiver needs to match when used as a mirror destination. Override to /v1/metrics for direct, non-mirrored OTLP callers. |
| receiver.http.port | int | `4318` | Port the OTLP/HTTP receiver listens on. |
| replicas | int | `3` | Number of Alloy replicas. Plain horizontal scaling behind the Service (round-robin) — Alloy clustering is not used because this pipeline has no discovery/scrape work to coordinate across peers; each replica independently receives, filters, and forwards. |
| resources.limits.memory | string | `"256Mi"` | No CPU limit is set: only a memory limit, to avoid CPU throttling. |
| resources.requests.cpu | string | `"50m"` |  |
| resources.requests.memory | string | `"128Mi"` |  |
| runAsUser | int | `10000` | Non-root UID the container runs as. The upstream grafana/alloy image defaults to root, so this must be set explicitly to satisfy the cluster's require-run-as-non-root-user policy. |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
